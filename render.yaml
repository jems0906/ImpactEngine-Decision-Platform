databases:
  - name: impactengine-db
    databaseName: impactengine
    user: impactengine
    plan: free

services:
  - type: web
    name: impactengine-backend
    runtime: docker
    dockerfilePath: ./backend/Dockerfile
    dockerContext: ./backend
    plan: free
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: impactengine-db
          property: connectionString
      - key: CORS_ORIGINS
        fromService:
          type: web
          name: impactengine-frontend
          property: url

  - type: web
    name: impactengine-frontend
    runtime: static
    rootDir: frontend
    buildCommand: npm install && npm run build
    staticPublishPath: dist
    plan: free
    envVars:
      - key: VITE_API_BASE_URL
        fromService:
          type: web
          name: impactengine-backend
          property: url
    headers:
      - path: /*
        name: Cache-Control
        value: no-cache
